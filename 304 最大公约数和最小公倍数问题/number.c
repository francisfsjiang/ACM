#include<stdio.h>
main()
{
      freopen("number.in","r",stdin);
      freopen("number.out","w",stdout);
      int x,y,i,n;
      scanf("%d%d",&x,&y);
      if(x<y)
      {
             int temp;
             temp=x;
             x=y;
             y=temp;
      }
      n=x/y;
      for()
      
